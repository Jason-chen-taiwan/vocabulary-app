# P2 內容層 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立字彙內容系統——TOEIC 單字書的資料模型、領域型別、repository、CardSource 抽象（為日後 AI 匯入預留乾淨擴充點）、可重複執行的種子載入器與一批真實多益起始單字，以及單字書瀏覽 / 單字詳情 / TTS 發音的 UI。

**Architecture:** 沿用 P1 的模組化分層。新增 `lib/content` 模組：Prisma 模型（WordBook/Word/Example）→ repository（將 Prisma row 映射為與框架解耦的領域型別）→ `CardSource` 介面與 `BuiltinWordBookSource` 實作。內容以靜態 JSON 種子檔載入（一次性離線生成、零 runtime AI 成本），透過冪等載入腳本 upsert 到 Neon。UI 為登入守衛下的 App Router server components，發音用瀏覽器 Web Speech API。

**Tech Stack:** Next.js 16 (App Router) · TypeScript · Tailwind · Prisma 7 + Neon · Vitest · Web Speech API

## Global Constraints

- 模組化優先：跨模組只透過介面與領域型別溝通，消費端不得依賴 Prisma 型別；不得 import 他模組內部實作。
- ALL DB 存取經 repository 層；業務邏輯不得出現原生 SQL。
- edge runtime：DB 走 Neon serverless driver（HTTP），非 TCP 連線池（已由 `lib/db/client.ts` 的 `getPrisma()` 處理）。
- 卡片來源以 `CardSource` 介面抽象；TOEIC 內建書是其一實作，日後 AI 匯入為另一實作——本層不得寫死「只有內建書」的假設。
- 不在 runtime 呼叫付費 AI / 付費 TTS：詞表與例句為離線生成的靜態種子；發音用瀏覽器 `speechSynthesis`（零成本）。
- 成本鐵則：只用免費或固定低月費、無超量爆帳單服務。
- 採 TDD：邏輯（映射、驗證、CardSource、TTS helper）先寫測試；schema / UI / 種子載入以可重現驗證指令驗收。
- 所有頁面登入守衛，與 P1 一致（`getCurrentUser()` 為 null 則 `redirect('/login')`）。
- UI 文案繁體中文。npm；Windows / Git Bash。DB 已上線（`.env` 有真實 `DATABASE_URL`）。

---

### Task 1: 內容資料模型與領域型別

**Files:**
- Modify: `prisma/schema.prisma`（新增 WordBook / Word / Example 三個 model）
- Create: `lib/content/types.ts`
- Create: `lib/content/__tests__/types.test.ts`

**Interfaces:**
- Consumes: 既有 Prisma datasource/generator（P1）
- Produces:
  - Prisma models：`WordBook`、`Word`、`Example`（欄位見下）。
  - 領域型別（與 Prisma 解耦，供所有上層使用）：
    - `ExampleData = { id: string; sentence: string; translationZh: string; source: string | null }`
    - `WordData = { id: string; headword: string; phonetic: string | null; partOfSpeech: string | null; definitionZh: string; examTags: string[] }`
    - `WordWithExamples = WordData & { examples: ExampleData[] }`
    - `WordBookData = { id: string; slug: string; name: string; description: string | null; level: string | null; wordCount: number }`

- [ ] **Step 1: 在 schema.prisma 新增三個 model**

在 `prisma/schema.prisma` 末尾新增（User/Account 等既有 model 不動）：
```prisma
model WordBook {
  id          String   @id @default(cuid())
  slug        String   @unique
  name        String
  description String?
  level       String?
  sourceType  String   @default("builtin")
  order       Int      @default(0)
  words       Word[]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model Word {
  id           String    @id @default(cuid())
  wordBookId   String
  headword     String
  phonetic     String?
  partOfSpeech String?
  definitionZh String
  examTags     String[]
  order        Int       @default(0)
  wordBook     WordBook  @relation(fields: [wordBookId], references: [id], onDelete: Cascade)
  examples     Example[]
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt

  @@unique([wordBookId, headword])
}

model Example {
  id            String   @id @default(cuid())
  wordId        String
  sentence      String
  translationZh String
  source        String?
  order         Int      @default(0)
  word          Word     @relation(fields: [wordId], references: [id], onDelete: Cascade)
}
```

- [ ] **Step 2: 產生 client 並推送 schema 到 Neon**

Run:
```
npm run db:generate
npm run db:push
```
Expected: 成功，無錯誤；Neon 出現 `WordBook`、`Word`、`Example` 三張新表（既有 User 等保留）。

- [ ] **Step 3: 寫領域型別映射測試（先失敗）**

Create `lib/content/__tests__/types.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { toWordWithExamples, toWordBookData } from '@/lib/content/types'

describe('toWordWithExamples', () => {
  it('maps a prisma word row (with examples) to the domain type', () => {
    const row = {
      id: 'w1', headword: 'invoice', phonetic: '/ˈɪnvɔɪs/', partOfSpeech: 'n.',
      definitionZh: '發票', examTags: ['TOEIC'],
      examples: [
        { id: 'e1', sentence: 'Send the invoice.', translationZh: '把發票寄出。', source: null, order: 0 },
      ],
      // extra prisma fields that must NOT leak through:
      wordBookId: 'b1', order: 0, createdAt: new Date(), updatedAt: new Date(),
    }
    expect(toWordWithExamples(row as any)).toEqual({
      id: 'w1', headword: 'invoice', phonetic: '/ˈɪnvɔɪs/', partOfSpeech: 'n.',
      definitionZh: '發票', examTags: ['TOEIC'],
      examples: [{ id: 'e1', sentence: 'Send the invoice.', translationZh: '把發票寄出。', source: null }],
    })
  })
})

describe('toWordBookData', () => {
  it('maps a book row with a word count', () => {
    const row = { id: 'b1', slug: 'toeic-core', name: '多益核心字彙', description: null, level: 'TOEIC', _count: { words: 12 } }
    expect(toWordBookData(row as any)).toEqual({
      id: 'b1', slug: 'toeic-core', name: '多益核心字彙', description: null, level: 'TOEIC', wordCount: 12,
    })
  })
})
```

- [ ] **Step 4: 執行確認失敗**

Run: `npm test -- types`
Expected: FAIL，找不到 `@/lib/content/types`。

- [ ] **Step 5: 實作領域型別與映射**

Create `lib/content/types.ts`:
```ts
export interface ExampleData {
  id: string
  sentence: string
  translationZh: string
  source: string | null
}

export interface WordData {
  id: string
  headword: string
  phonetic: string | null
  partOfSpeech: string | null
  definitionZh: string
  examTags: string[]
}

export interface WordWithExamples extends WordData {
  examples: ExampleData[]
}

export interface WordBookData {
  id: string
  slug: string
  name: string
  description: string | null
  level: string | null
  wordCount: number
}

// Prisma-row shapes we map FROM (only the fields we read).
type ExampleRow = { id: string; sentence: string; translationZh: string; source: string | null }
type WordRow = {
  id: string; headword: string; phonetic: string | null; partOfSpeech: string | null
  definitionZh: string; examTags: string[]; examples?: ExampleRow[]
}
type WordBookRow = {
  id: string; slug: string; name: string; description: string | null
  level: string | null; _count?: { words: number }
}

export function toExampleData(row: ExampleRow): ExampleData {
  return { id: row.id, sentence: row.sentence, translationZh: row.translationZh, source: row.source }
}

export function toWordData(row: WordRow): WordData {
  return {
    id: row.id, headword: row.headword, phonetic: row.phonetic,
    partOfSpeech: row.partOfSpeech, definitionZh: row.definitionZh, examTags: row.examTags,
  }
}

export function toWordWithExamples(row: WordRow): WordWithExamples {
  return { ...toWordData(row), examples: (row.examples ?? []).map(toExampleData) }
}

export function toWordBookData(row: WordBookRow): WordBookData {
  return {
    id: row.id, slug: row.slug, name: row.name, description: row.description,
    level: row.level, wordCount: row._count?.words ?? 0,
  }
}
```

- [ ] **Step 6: 執行確認通過**

Run: `npm test -- types`
Expected: PASS（2 passed）。

- [ ] **Step 7: 確認全套與 build 仍綠**

Run: `npm test && npm run build`
Expected: 全部測試通過；build exit 0。

- [ ] **Step 8: Commit**

```
git add prisma/schema.prisma lib/content/types.ts lib/content/__tests__/types.test.ts
git commit -m "feat(content): add WordBook/Word/Example models and domain types"
```

---

### Task 2: 內容 Repository

**Files:**
- Create: `lib/content/repository.ts`
- Create: `lib/content/__tests__/repository.test.ts`

**Interfaces:**
- Consumes: Task 1 領域型別與映射；P1 `getPrisma()`（`@/lib/db/client`）。
- Produces: `ContentRepository`，建構子可注入 prisma-like 物件（預設 `getPrisma()`），方法：
  - `listWordBooks(): Promise<WordBookData[]>`（依 `order` 升冪，含 `_count.words`）
  - `getWordBookBySlug(slug: string): Promise<WordBookData | null>`
  - `listWordsByBook(wordBookId: string): Promise<WordData[]>`（依 `order` 升冪）
  - `getWordWithExamples(wordId: string): Promise<WordWithExamples | null>`（含 examples，依 `order` 升冪）

- [ ] **Step 1: 寫 repository 測試（先失敗）**

Create `lib/content/__tests__/repository.test.ts`:
```ts
import { describe, it, expect, vi } from 'vitest'
import { ContentRepository } from '@/lib/content/repository'

function makeDb() {
  return {
    wordBook: { findMany: vi.fn(), findUnique: vi.fn() },
    word: { findMany: vi.fn(), findUnique: vi.fn() },
  }
}

describe('ContentRepository', () => {
  it('listWordBooks orders by order asc and includes word count', async () => {
    const db = makeDb()
    db.wordBook.findMany.mockResolvedValue([
      { id: 'b1', slug: 'toeic-core', name: '多益核心字彙', description: null, level: 'TOEIC', _count: { words: 2 } },
    ])
    const repo = new ContentRepository(db as any)
    const result = await repo.listWordBooks()
    expect(db.wordBook.findMany).toHaveBeenCalledWith({
      orderBy: { order: 'asc' },
      include: { _count: { select: { words: true } } },
    })
    expect(result).toEqual([
      { id: 'b1', slug: 'toeic-core', name: '多益核心字彙', description: null, level: 'TOEIC', wordCount: 2 },
    ])
  })

  it('getWordBookBySlug returns null when not found', async () => {
    const db = makeDb()
    db.wordBook.findUnique.mockResolvedValue(null)
    const repo = new ContentRepository(db as any)
    expect(await repo.getWordBookBySlug('missing')).toBeNull()
    expect(db.wordBook.findUnique).toHaveBeenCalledWith({
      where: { slug: 'missing' },
      include: { _count: { select: { words: true } } },
    })
  })

  it('listWordsByBook maps rows to WordData ordered by order asc', async () => {
    const db = makeDb()
    db.word.findMany.mockResolvedValue([
      { id: 'w1', headword: 'invoice', phonetic: null, partOfSpeech: 'n.', definitionZh: '發票', examTags: ['TOEIC'] },
    ])
    const repo = new ContentRepository(db as any)
    const result = await repo.listWordsByBook('b1')
    expect(db.word.findMany).toHaveBeenCalledWith({ where: { wordBookId: 'b1' }, orderBy: { order: 'asc' } })
    expect(result).toEqual([
      { id: 'w1', headword: 'invoice', phonetic: null, partOfSpeech: 'n.', definitionZh: '發票', examTags: ['TOEIC'] },
    ])
  })

  it('getWordWithExamples includes examples ordered by order asc', async () => {
    const db = makeDb()
    db.word.findUnique.mockResolvedValue({
      id: 'w1', headword: 'invoice', phonetic: null, partOfSpeech: 'n.', definitionZh: '發票', examTags: [],
      examples: [{ id: 'e1', sentence: 'X.', translationZh: 'X。', source: null }],
    })
    const repo = new ContentRepository(db as any)
    const result = await repo.getWordWithExamples('w1')
    expect(db.word.findUnique).toHaveBeenCalledWith({
      where: { id: 'w1' },
      include: { examples: { orderBy: { order: 'asc' } } },
    })
    expect(result?.examples).toEqual([{ id: 'e1', sentence: 'X.', translationZh: 'X。', source: null }])
  })
})
```

- [ ] **Step 2: 執行確認失敗**

Run: `npm test -- content/__tests__/repository`
Expected: FAIL，找不到 `@/lib/content/repository`。

- [ ] **Step 3: 實作 repository**

Create `lib/content/repository.ts`:
```ts
import { getPrisma } from '@/lib/db/client'
import {
  toWordBookData, toWordData, toWordWithExamples,
  type WordBookData, type WordData, type WordWithExamples,
} from './types'

// Minimal structural type of the Prisma delegates we use — lets tests inject a mock.
interface ContentDb {
  wordBook: {
    findMany(args: unknown): Promise<unknown[]>
    findUnique(args: unknown): Promise<unknown | null>
  }
  word: {
    findMany(args: unknown): Promise<unknown[]>
    findUnique(args: unknown): Promise<unknown | null>
  }
}

export class ContentRepository {
  private readonly db: ContentDb
  constructor(db?: ContentDb) {
    this.db = db ?? (getPrisma() as unknown as ContentDb)
  }

  async listWordBooks(): Promise<WordBookData[]> {
    const rows = await this.db.wordBook.findMany({
      orderBy: { order: 'asc' },
      include: { _count: { select: { words: true } } },
    })
    return (rows as Parameters<typeof toWordBookData>[0][]).map(toWordBookData)
  }

  async getWordBookBySlug(slug: string): Promise<WordBookData | null> {
    const row = await this.db.wordBook.findUnique({
      where: { slug },
      include: { _count: { select: { words: true } } },
    })
    return row ? toWordBookData(row as Parameters<typeof toWordBookData>[0]) : null
  }

  async listWordsByBook(wordBookId: string): Promise<WordData[]> {
    const rows = await this.db.word.findMany({ where: { wordBookId }, orderBy: { order: 'asc' } })
    return (rows as Parameters<typeof toWordData>[0][]).map(toWordData)
  }

  async getWordWithExamples(wordId: string): Promise<WordWithExamples | null> {
    const row = await this.db.word.findUnique({
      where: { id: wordId },
      include: { examples: { orderBy: { order: 'asc' } } },
    })
    return row ? toWordWithExamples(row as Parameters<typeof toWordWithExamples>[0]) : null
  }
}
```

- [ ] **Step 4: 執行確認通過**

Run: `npm test -- content/__tests__/repository`
Expected: PASS（4 passed）。

- [ ] **Step 5: Commit**

```
git add lib/content/repository.ts lib/content/__tests__/repository.test.ts
git commit -m "feat(content): add ContentRepository with injectable prisma delegate"
```

---

### Task 3: CardSource 抽象與 BuiltinWordBookSource

**Files:**
- Create: `lib/content/card-source.ts`
- Create: `lib/content/__tests__/card-source.test.ts`

**Interfaces:**
- Consumes: Task 1 `WordWithExamples`；Task 2 `ContentRepository`。
- Produces:
  - `interface CardSource { readonly id: string; listCards(): Promise<WordWithExamples[]> }`
  - `class BuiltinWordBookSource implements CardSource`：建構子 `(slug: string, repo?: ContentRepository)`；`id` 為 `builtin:<slug>`；`listCards()` 取得該書所有單字（含 examples）。
  - 這是日後 AI 匯入的擴充點：未來新增 `AiImportSource implements CardSource` 即可，學習層只依賴 `CardSource` 介面。

- [ ] **Step 1: 寫測試（先失敗）**

Create `lib/content/__tests__/card-source.test.ts`:
```ts
import { describe, it, expect, vi } from 'vitest'
import { BuiltinWordBookSource } from '@/lib/content/card-source'

describe('BuiltinWordBookSource', () => {
  it('id is namespaced by slug', () => {
    const src = new BuiltinWordBookSource('toeic-core', {} as any)
    expect(src.id).toBe('builtin:toeic-core')
  })

  it('listCards resolves the book by slug then returns its words with examples', async () => {
    const repo = {
      getWordBookBySlug: vi.fn().mockResolvedValue({ id: 'b1', slug: 'toeic-core', name: 'x', description: null, level: null, wordCount: 1 }),
      listWordsByBook: vi.fn().mockResolvedValue([{ id: 'w1', headword: 'invoice', phonetic: null, partOfSpeech: 'n.', definitionZh: '發票', examTags: [] }]),
      getWordWithExamples: vi.fn().mockResolvedValue({ id: 'w1', headword: 'invoice', phonetic: null, partOfSpeech: 'n.', definitionZh: '發票', examTags: [], examples: [{ id: 'e1', sentence: 'X.', translationZh: 'X。', source: null }] }),
    }
    const src = new BuiltinWordBookSource('toeic-core', repo as any)
    const cards = await src.listCards()
    expect(repo.getWordBookBySlug).toHaveBeenCalledWith('toeic-core')
    expect(repo.listWordsByBook).toHaveBeenCalledWith('b1')
    expect(cards).toHaveLength(1)
    expect(cards[0].examples[0].sentence).toBe('X.')
  })

  it('listCards returns [] when the book does not exist', async () => {
    const repo = { getWordBookBySlug: vi.fn().mockResolvedValue(null), listWordsByBook: vi.fn(), getWordWithExamples: vi.fn() }
    const src = new BuiltinWordBookSource('missing', repo as any)
    expect(await src.listCards()).toEqual([])
    expect(repo.listWordsByBook).not.toHaveBeenCalled()
  })
})
```

- [ ] **Step 2: 執行確認失敗**

Run: `npm test -- card-source`
Expected: FAIL，找不到 `@/lib/content/card-source`。

- [ ] **Step 3: 實作**

Create `lib/content/card-source.ts`:
```ts
import { ContentRepository } from './repository'
import type { WordWithExamples } from './types'

export interface CardSource {
  readonly id: string
  listCards(): Promise<WordWithExamples[]>
}

export class BuiltinWordBookSource implements CardSource {
  readonly id: string
  private readonly slug: string
  private readonly repo: ContentRepository

  constructor(slug: string, repo: ContentRepository = new ContentRepository()) {
    this.slug = slug
    this.repo = repo
    this.id = `builtin:${slug}`
  }

  async listCards(): Promise<WordWithExamples[]> {
    const book = await this.repo.getWordBookBySlug(this.slug)
    if (!book) return []
    const words = await this.repo.listWordsByBook(book.id)
    const cards = await Promise.all(words.map((w) => this.repo.getWordWithExamples(w.id)))
    return cards.filter((c): c is WordWithExamples => c !== null)
  }
}
```

- [ ] **Step 4: 執行確認通過**

Run: `npm test -- card-source`
Expected: PASS（3 passed）。

- [ ] **Step 5: Commit**

```
git add lib/content/card-source.ts lib/content/__tests__/card-source.test.ts
git commit -m "feat(content): add CardSource interface and BuiltinWordBookSource"
```

---

### Task 4: 種子格式驗證、載入腳本與多益起始內容

**Files:**
- Create: `lib/content/seed-schema.ts`
- Create: `lib/content/__tests__/seed-schema.test.ts`
- Create: `content/toeic-core.json`（真實多益起始單字）
- Create: `scripts/seed-content.mjs`
- Modify: `package.json`（加 `"seed": "node scripts/seed-content.mjs"`）

**Interfaces:**
- Consumes: Task 1 schema（live Neon）；P1 `getPrisma()`。
- Produces:
  - `parseSeedBook(raw: unknown): SeedBook`（純函式，驗證並回傳；格式不符則 throw 明確訊息）。型別：
    - `SeedExample = { sentence: string; translationZh: string; source?: string }`
    - `SeedWord = { headword: string; phonetic?: string; partOfSpeech?: string; definitionZh: string; examTags?: string[]; examples: SeedExample[] }`
    - `SeedBook = { slug: string; name: string; description?: string; level?: string; words: SeedWord[] }`
  - 冪等載入腳本 `npm run seed`：依 slug upsert WordBook，依 `(wordBookId, headword)` upsert Word，每個 Word 先刪除既有 examples 再插入（冪等）。order 依陣列索引。

- [ ] **Step 1: 寫驗證測試（先失敗）**

Create `lib/content/__tests__/seed-schema.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { parseSeedBook } from '@/lib/content/seed-schema'

const valid = {
  slug: 'toeic-core', name: '多益核心字彙', level: 'TOEIC',
  words: [
    { headword: 'invoice', phonetic: '/ˈɪnvɔɪs/', partOfSpeech: 'n.', definitionZh: '發票', examTags: ['TOEIC'],
      examples: [{ sentence: 'Send the invoice.', translationZh: '把發票寄出。' }] },
  ],
}

describe('parseSeedBook', () => {
  it('accepts a valid book and defaults optional fields', () => {
    const book = parseSeedBook(valid)
    expect(book.slug).toBe('toeic-core')
    expect(book.words[0].examTags).toEqual(['TOEIC'])
    expect(book.words[0].examples[0].source).toBeUndefined()
  })

  it('throws when slug is missing', () => {
    expect(() => parseSeedBook({ ...valid, slug: '' })).toThrowError(/slug/)
  })

  it('throws when a word has no examples', () => {
    const bad = { ...valid, words: [{ headword: 'x', definitionZh: 'x', examples: [] }] }
    expect(() => parseSeedBook(bad)).toThrowError(/examples/)
  })

  it('throws when a word is missing definitionZh', () => {
    const bad = { ...valid, words: [{ headword: 'x', examples: [{ sentence: 'a', translationZh: 'b' }] }] }
    expect(() => parseSeedBook(bad)).toThrowError(/definitionZh/)
  })
})
```

- [ ] **Step 2: 執行確認失敗**

Run: `npm test -- seed-schema`
Expected: FAIL，找不到 `@/lib/content/seed-schema`。

- [ ] **Step 3: 實作驗證器**

Create `lib/content/seed-schema.ts`:
```ts
export interface SeedExample { sentence: string; translationZh: string; source?: string }
export interface SeedWord {
  headword: string
  phonetic?: string
  partOfSpeech?: string
  definitionZh: string
  examTags?: string[]
  examples: SeedExample[]
}
export interface SeedBook {
  slug: string
  name: string
  description?: string
  level?: string
  words: SeedWord[]
}

function req(cond: boolean, msg: string): void {
  if (!cond) throw new Error(`Invalid seed: ${msg}`)
}

function parseExample(raw: unknown, where: string): SeedExample {
  const e = raw as Record<string, unknown>
  req(typeof e?.sentence === 'string' && e.sentence.length > 0, `${where}.sentence must be a non-empty string`)
  req(typeof e?.translationZh === 'string' && e.translationZh.length > 0, `${where}.translationZh must be a non-empty string`)
  req(e.source === undefined || typeof e.source === 'string', `${where}.source must be a string if present`)
  return { sentence: e.sentence as string, translationZh: e.translationZh as string, source: e.source as string | undefined }
}

function parseWord(raw: unknown, i: number): SeedWord {
  const w = raw as Record<string, unknown>
  const where = `words[${i}]`
  req(typeof w?.headword === 'string' && w.headword.length > 0, `${where}.headword must be a non-empty string`)
  req(typeof w?.definitionZh === 'string' && w.definitionZh.length > 0, `${where}.definitionZh must be a non-empty string`)
  req(Array.isArray(w.examples) && w.examples.length > 0, `${where}.examples must be a non-empty array`)
  req(w.examTags === undefined || Array.isArray(w.examTags), `${where}.examTags must be an array if present`)
  return {
    headword: w.headword as string,
    phonetic: w.phonetic as string | undefined,
    partOfSpeech: w.partOfSpeech as string | undefined,
    definitionZh: w.definitionZh as string,
    examTags: (w.examTags as string[] | undefined) ?? [],
    examples: (w.examples as unknown[]).map((e, j) => parseExample(e, `${where}.examples[${j}]`)),
  }
}

export function parseSeedBook(raw: unknown): SeedBook {
  const b = raw as Record<string, unknown>
  req(typeof b?.slug === 'string' && b.slug.length > 0, 'slug must be a non-empty string')
  req(typeof b?.name === 'string' && b.name.length > 0, 'name must be a non-empty string')
  req(Array.isArray(b.words) && b.words.length > 0, 'words must be a non-empty array')
  return {
    slug: b.slug as string,
    name: b.name as string,
    description: b.description as string | undefined,
    level: b.level as string | undefined,
    words: (b.words as unknown[]).map(parseWord),
  }
}
```

- [ ] **Step 4: 執行確認通過**

Run: `npm test -- seed-schema`
Expected: PASS（4 passed）。

- [ ] **Step 5: 建立多益起始內容 JSON**

Create `content/toeic-core.json`（真實、可直接出貨的起始批次；之後可附加更多 words 再重跑 seed）:
```json
{
  "slug": "toeic-core",
  "name": "多益核心字彙",
  "description": "職場與商務情境最常見的多益高頻單字",
  "level": "TOEIC",
  "words": [
    { "headword": "negotiate", "phonetic": "/nɪˈɡoʊʃieɪt/", "partOfSpeech": "v.", "definitionZh": "談判，協商", "examTags": ["TOEIC"], "examples": [{ "sentence": "We need to negotiate the terms of the contract.", "translationZh": "我們需要協商合約的條款。" }] },
    { "headword": "invoice", "phonetic": "/ˈɪnvɔɪs/", "partOfSpeech": "n.", "definitionZh": "發票，請款單", "examTags": ["TOEIC"], "examples": [{ "sentence": "Please send the invoice to the accounting department.", "translationZh": "請將發票寄給會計部門。" }] },
    { "headword": "deadline", "phonetic": "/ˈdedlaɪn/", "partOfSpeech": "n.", "definitionZh": "截止期限", "examTags": ["TOEIC"], "examples": [{ "sentence": "The deadline for the report is this Friday.", "translationZh": "報告的截止期限是這週五。" }] },
    { "headword": "colleague", "phonetic": "/ˈkɑːliːɡ/", "partOfSpeech": "n.", "definitionZh": "同事", "examTags": ["TOEIC"], "examples": [{ "sentence": "She introduced me to her new colleague.", "translationZh": "她把我介紹給她的新同事。" }] },
    { "headword": "schedule", "phonetic": "/ˈskedʒuːl/", "partOfSpeech": "n./v.", "definitionZh": "時間表；安排", "examTags": ["TOEIC"], "examples": [{ "sentence": "Let's schedule a meeting for next week.", "translationZh": "我們安排下週開個會吧。" }] },
    { "headword": "budget", "phonetic": "/ˈbʌdʒɪt/", "partOfSpeech": "n.", "definitionZh": "預算", "examTags": ["TOEIC"], "examples": [{ "sentence": "The marketing project went over budget.", "translationZh": "這個行銷專案超出了預算。" }] },
    { "headword": "client", "phonetic": "/ˈklaɪənt/", "partOfSpeech": "n.", "definitionZh": "客戶", "examTags": ["TOEIC"], "examples": [{ "sentence": "Our client requested a full refund.", "translationZh": "我們的客戶要求全額退款。" }] },
    { "headword": "shipment", "phonetic": "/ˈʃɪpmənt/", "partOfSpeech": "n.", "definitionZh": "裝運，運送的貨物", "examTags": ["TOEIC"], "examples": [{ "sentence": "The shipment will arrive on Monday morning.", "translationZh": "這批貨將在週一早上抵達。" }] },
    { "headword": "warranty", "phonetic": "/ˈwɔːrənti/", "partOfSpeech": "n.", "definitionZh": "保固，保證書", "examTags": ["TOEIC"], "examples": [{ "sentence": "This laptop comes with a two-year warranty.", "translationZh": "這台筆電附有兩年保固。" }] },
    { "headword": "reimburse", "phonetic": "/ˌriːɪmˈbɜːrs/", "partOfSpeech": "v.", "definitionZh": "償還，報銷", "examTags": ["TOEIC"], "examples": [{ "sentence": "The company will reimburse your travel expenses.", "translationZh": "公司會報銷你的差旅費。" }] },
    { "headword": "agenda", "phonetic": "/əˈdʒendə/", "partOfSpeech": "n.", "definitionZh": "議程", "examTags": ["TOEIC"], "examples": [{ "sentence": "The first item on the agenda is the annual budget.", "translationZh": "議程的第一項是年度預算。" }] },
    { "headword": "vendor", "phonetic": "/ˈvendər/", "partOfSpeech": "n.", "definitionZh": "供應商，賣方", "examTags": ["TOEIC"], "examples": [{ "sentence": "We signed a contract with a new vendor.", "translationZh": "我們和一家新供應商簽了合約。" }] }
  ]
}
```

- [ ] **Step 6: 建立冪等載入腳本**

Create `scripts/seed-content.mjs`:
```js
import 'dotenv/config'
import { readFileSync } from 'node:fs'
import { PrismaClient } from '@prisma/client'
import { PrismaNeonHttp } from '@prisma/adapter-neon'
import { parseSeedBook } from '../lib/content/seed-schema.ts'

const file = process.argv[2] ?? 'content/toeic-core.json'
const book = parseSeedBook(JSON.parse(readFileSync(file, 'utf8')))

const adapter = new PrismaNeonHttp(process.env.DATABASE_URL, {})
const prisma = new PrismaClient({ adapter })

const wb = await prisma.wordBook.upsert({
  where: { slug: book.slug },
  update: { name: book.name, description: book.description ?? null, level: book.level ?? null },
  create: { slug: book.slug, name: book.name, description: book.description ?? null, level: book.level ?? null },
})

let n = 0
for (let i = 0; i < book.words.length; i++) {
  const w = book.words[i]
  const word = await prisma.word.upsert({
    where: { wordBookId_headword: { wordBookId: wb.id, headword: w.headword } },
    update: { phonetic: w.phonetic ?? null, partOfSpeech: w.partOfSpeech ?? null, definitionZh: w.definitionZh, examTags: w.examTags ?? [], order: i },
    create: { wordBookId: wb.id, headword: w.headword, phonetic: w.phonetic ?? null, partOfSpeech: w.partOfSpeech ?? null, definitionZh: w.definitionZh, examTags: w.examTags ?? [], order: i },
  })
  // examples: delete + re-insert for idempotency
  await prisma.example.deleteMany({ where: { wordId: word.id } })
  await prisma.example.createMany({
    data: w.examples.map((e, j) => ({ wordId: word.id, sentence: e.sentence, translationZh: e.translationZh, source: e.source ?? null, order: j })),
  })
  n++
}

console.log(`Seeded book "${book.slug}" with ${n} words.`)
await prisma.$disconnect()
```
> 註：腳本以 `.ts` import 驗證器；用 Node v25 原生 TS 載入（本專案 Node v25 支援 `--experimental-strip-types`，或腳本以 `node` 直接執行 `.mjs` 並 import `.ts` 若環境支援）。若 `node scripts/seed-content.mjs` 因匯入 `.ts` 失敗，改用 `node --experimental-strip-types scripts/seed-content.mjs`，並在 `package.json` 的 seed script 加上該旗標。

- [ ] **Step 7: 加 seed script 並執行**

Modify `package.json` `"scripts"` 加入：
```json
"seed": "node scripts/seed-content.mjs"
```
Run（DB 已上線）:
```
npm run seed
```
Expected: 輸出 `Seeded book "toeic-core" with 12 words.`，無錯誤。再跑一次應仍成功（冪等，不重複建立、不報錯）。

- [ ] **Step 8: 驗證內容已入庫**

Run:
```
node -e "import('dotenv/config').then(async()=>{const{neon}=await import('@neondatabase/serverless');const sql=neon(process.env.DATABASE_URL);const b=await sql\`SELECT slug,name FROM \"WordBook\"\`;const w=await sql\`SELECT count(*)::int n FROM \"Word\"\`;const e=await sql\`SELECT count(*)::int n FROM \"Example\"\`;console.log('books',b,'words',w[0].n,'examples',e[0].n);})"
```
Expected: 1 本書 `toeic-core`、12 個 words、12 個 examples。

- [ ] **Step 9: Commit**

```
git add lib/content/seed-schema.ts lib/content/__tests__/seed-schema.test.ts content/toeic-core.json scripts/seed-content.mjs package.json
git commit -m "feat(content): add seed validator, idempotent loader, and TOEIC starter content"
```

---

### Task 5: 單字書列表與詳情頁

**Files:**
- Create: `app/books/page.tsx`
- Create: `app/books/[slug]/page.tsx`
- Modify: `app/page.tsx`（首頁加入前往單字書的連結）

**Interfaces:**
- Consumes: P1 `getCurrentUser()`（`@/lib/auth/session`）；Task 2 `ContentRepository`。
- Produces: `/books`（列出所有單字書）、`/books/[slug]`（列出該書單字，連到單字詳情 `/words/[id]`，Task 6 建立）。皆登入守衛。

- [ ] **Step 1: 建立單字書列表頁**

Create `app/books/page.tsx`:
```tsx
import Link from 'next/link'
import { redirect } from 'next/navigation'
import { getCurrentUser } from '@/lib/auth/session'
import { ContentRepository } from '@/lib/content/repository'

export default async function BooksPage() {
  if (!(await getCurrentUser())) redirect('/login')
  const books = await new ContentRepository().listWordBooks()
  return (
    <main className="mx-auto max-w-2xl px-4 py-8">
      <h1 className="mb-6 text-2xl font-bold">單字書</h1>
      {books.length === 0 ? (
        <p className="text-gray-500">目前還沒有單字書。</p>
      ) : (
        <ul className="space-y-3">
          {books.map((b) => (
            <li key={b.id}>
              <Link href={`/books/${b.slug}`} className="block rounded-lg border border-gray-700 p-4 hover:bg-gray-900">
                <div className="font-semibold">{b.name}</div>
                {b.description && <div className="text-sm text-gray-400">{b.description}</div>}
                <div className="mt-1 text-xs text-gray-500">{b.wordCount} 個單字{b.level ? ` · ${b.level}` : ''}</div>
              </Link>
            </li>
          ))}
        </ul>
      )}
    </main>
  )
}
```

- [ ] **Step 2: 建立單字書詳情頁**

Create `app/books/[slug]/page.tsx`:
```tsx
import Link from 'next/link'
import { notFound, redirect } from 'next/navigation'
import { getCurrentUser } from '@/lib/auth/session'
import { ContentRepository } from '@/lib/content/repository'

export default async function BookDetailPage({ params }: { params: Promise<{ slug: string }> }) {
  if (!(await getCurrentUser())) redirect('/login')
  const { slug } = await params
  const repo = new ContentRepository()
  const book = await repo.getWordBookBySlug(slug)
  if (!book) notFound()
  const words = await repo.listWordsByBook(book.id)
  return (
    <main className="mx-auto max-w-2xl px-4 py-8">
      <Link href="/books" className="text-sm text-gray-400 hover:underline">← 所有單字書</Link>
      <h1 className="mt-2 mb-6 text-2xl font-bold">{book.name}</h1>
      <ul className="divide-y divide-gray-800">
        {words.map((w) => (
          <li key={w.id}>
            <Link href={`/words/${w.id}`} className="flex items-baseline justify-between py-3 hover:bg-gray-900">
              <span className="font-medium">{w.headword}</span>
              <span className="ml-4 truncate text-sm text-gray-400">{w.definitionZh}</span>
            </Link>
          </li>
        ))}
      </ul>
    </main>
  )
}
```

- [ ] **Step 3: 首頁加入單字書連結**

Modify `app/page.tsx`：在歡迎標題下方、登出按鈕附近，加入一個前往 `/books` 的連結。於 `import` 區加上 `import Link from 'next/link'`，並在 `<h1>歡迎...</h1>` 之後插入：
```tsx
<Link href="/books" className="rounded-lg bg-white px-4 py-2 font-medium text-black hover:bg-gray-200">
  開始學單字
</Link>
```
（保留既有的 `<SignOutButton />`。）

- [ ] **Step 4: 驗證 build 與路由**

Run: `npm run build`
Expected: exit 0；輸出的路由清單包含 `/books` 與 `/books/[slug]`。

- [ ] **Step 5: 手動驗證（dev）**

Run: `npm run dev`，登入後瀏覽 `http://localhost:3000/books`
Expected: 看到「多益核心字彙」書卡（顯示 12 個單字）；點進去看到 12 個單字列表（headword + 中文釋義）。

- [ ] **Step 6: Commit**

```
git add app/books app/page.tsx
git commit -m "feat(content): add word-book list and detail pages"
```

---

### Task 6: 單字詳情頁與 TTS 發音

**Files:**
- Create: `lib/content/tts.ts`
- Create: `lib/content/__tests__/tts.test.ts`
- Create: `components/tts-button.tsx`
- Create: `app/words/[id]/page.tsx`

**Interfaces:**
- Consumes: P1 `getCurrentUser()`；Task 2 `ContentRepository`（`getWordWithExamples`）。
- Produces:
  - `buildUtterance(text: string, lang?: string): { text: string; lang: string; rate: number }`（純函式，預設 `lang='en-US'`、`rate=0.9`）。
  - `<TtsButton text=... />` client 元件：用 `window.speechSynthesis` 朗讀；不支援時按鈕停用。
  - `/words/[id]` 單字詳情頁（headword、音標、詞性、釋義、例句+翻譯、發音鈕），登入守衛。

- [ ] **Step 1: 寫 TTS helper 測試（先失敗）**

Create `lib/content/__tests__/tts.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { buildUtterance } from '@/lib/content/tts'

describe('buildUtterance', () => {
  it('defaults to en-US and a slightly slow rate', () => {
    expect(buildUtterance('invoice')).toEqual({ text: 'invoice', lang: 'en-US', rate: 0.9 })
  })
  it('accepts a custom lang', () => {
    expect(buildUtterance('hello', 'en-GB')).toEqual({ text: 'hello', lang: 'en-GB', rate: 0.9 })
  })
})
```

- [ ] **Step 2: 執行確認失敗**

Run: `npm test -- content/__tests__/tts`
Expected: FAIL，找不到 `@/lib/content/tts`。

- [ ] **Step 3: 實作 helper**

Create `lib/content/tts.ts`:
```ts
export interface UtteranceConfig {
  text: string
  lang: string
  rate: number
}

export function buildUtterance(text: string, lang = 'en-US'): UtteranceConfig {
  return { text, lang, rate: 0.9 }
}
```

- [ ] **Step 4: 執行確認通過**

Run: `npm test -- content/__tests__/tts`
Expected: PASS（2 passed）。

- [ ] **Step 5: 建立 TTS 按鈕元件**

Create `components/tts-button.tsx`:
```tsx
'use client'
import { useEffect, useState } from 'react'
import { buildUtterance } from '@/lib/content/tts'

export function TtsButton({ text }: { text: string }) {
  const [supported, setSupported] = useState(false)
  useEffect(() => {
    setSupported(typeof window !== 'undefined' && 'speechSynthesis' in window)
  }, [])

  function speak() {
    const cfg = buildUtterance(text)
    const u = new SpeechSynthesisUtterance(cfg.text)
    u.lang = cfg.lang
    u.rate = cfg.rate
    window.speechSynthesis.cancel()
    window.speechSynthesis.speak(u)
  }

  return (
    <button
      type="button"
      onClick={speak}
      disabled={!supported}
      aria-label={`播放「${text}」的發音`}
      className="rounded-md border border-gray-600 px-2 py-1 text-sm hover:bg-gray-800 disabled:opacity-40"
    >
      🔊 發音
    </button>
  )
}
```

- [ ] **Step 6: 建立單字詳情頁**

Create `app/words/[id]/page.tsx`:
```tsx
import { notFound, redirect } from 'next/navigation'
import { getCurrentUser } from '@/lib/auth/session'
import { ContentRepository } from '@/lib/content/repository'
import { TtsButton } from '@/components/tts-button'

export default async function WordDetailPage({ params }: { params: Promise<{ id: string }> }) {
  if (!(await getCurrentUser())) redirect('/login')
  const { id } = await params
  const word = await new ContentRepository().getWordWithExamples(id)
  if (!word) notFound()
  return (
    <main className="mx-auto max-w-2xl px-4 py-8">
      <div className="flex items-center gap-3">
        <h1 className="text-3xl font-bold">{word.headword}</h1>
        <TtsButton text={word.headword} />
      </div>
      <div className="mt-1 text-gray-400">
        {word.phonetic && <span className="mr-2">{word.phonetic}</span>}
        {word.partOfSpeech && <span className="text-gray-500">{word.partOfSpeech}</span>}
      </div>
      <p className="mt-3 text-lg">{word.definitionZh}</p>
      <h2 className="mt-6 mb-2 text-sm font-semibold text-gray-400">例句</h2>
      <ul className="space-y-4">
        {word.examples.map((e) => (
          <li key={e.id} className="rounded-lg border border-gray-800 p-3">
            <div className="flex items-start gap-2">
              <p className="flex-1">{e.sentence}</p>
              <TtsButton text={e.sentence} />
            </div>
            <p className="mt-1 text-sm text-gray-400">{e.translationZh}</p>
          </li>
        ))}
      </ul>
    </main>
  )
}
```

- [ ] **Step 7: 驗證 build 與全套測試**

Run: `npm test && npm run build`
Expected: 全部測試通過；build exit 0；路由含 `/words/[id]`。

- [ ] **Step 8: 手動驗證（dev）**

Run: `npm run dev`，登入後從 `/books/toeic-core` 點任一單字
Expected: 看到單字、音標、詞性、釋義、例句+翻譯；按「🔊 發音」會用瀏覽器朗讀英文（單字與例句皆可）。

- [ ] **Step 9: Commit**

```
git add lib/content/tts.ts lib/content/__tests__/tts.test.ts components/tts-button.tsx app/words
git commit -m "feat(content): add word detail page with Web Speech TTS"
```

---

## Self-Review

**Spec coverage（P2 範圍）：**
- WordBook/Word/Example 資料模型 → Task 1 ✅
- 領域型別與框架解耦（模組化原則）→ Task 1 `types.ts` ✅
- 內容 repository（DB 存取收斂）→ Task 2 ✅
- CardSource 抽象 + 內建書實作（AI 匯入擴充點）→ Task 3 ✅
- 靜態種子內容、冪等載入、零 runtime AI 成本 → Task 4 ✅
- TOEIC 起始單字書 → Task 4 `toeic-core.json` ✅
- 單字書瀏覽 / 單字詳情 UI → Task 5、6 ✅
- TTS 發音（瀏覽器、零成本）→ Task 6 ✅
- 登入守衛、繁中文案、repository 收斂、TDD → Global Constraints 並落實各 Task ✅
- FSRS / 複習迴圈 / 遊戲化 / 統計：屬 P3–P5，不在本計畫範圍。

**內容量說明（非缺口，刻意設計）：** 本計畫交付完整內容「系統」+ 12 個真實多益單字作為可運作起始批次。詞庫擴充為可持續的資料工作：附加更多 words 到 `content/toeic-core.json`（或新增其他書的 JSON）後重跑 `npm run seed` 即可，軟體不需改動。建議 P2 完成後另跑一次 Claude 大量生成 + 校對來擴充詞庫。

**Placeholder scan：** 無 TBD/TODO；每個 code step 均含完整程式碼與確切指令/預期輸出。

**Type consistency：** `WordData`/`WordBookData`/`WordWithExamples`/`ExampleData`、`toWordWithExamples`/`toWordBookData`/`toWordData`、`ContentRepository`（`listWordBooks`/`getWordBookBySlug`/`listWordsByBook`/`getWordWithExamples`）、`CardSource`/`BuiltinWordBookSource`、`parseSeedBook`/`SeedBook`、`buildUtterance`/`UtteranceConfig` 於定義與消費處名稱一致。`prisma.word.upsert` 的複合唯一鍵 `wordBookId_headword` 對應 schema 的 `@@unique([wordBookId, headword])`。
